from django.apps import AppConfig


class PreventiveJobsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'preventive_jobs'
    verbose_name = 'Preventive Job Management (V2)'
